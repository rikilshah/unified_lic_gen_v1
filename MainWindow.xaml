<Window
    x:Class="UnifiedLicGen.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource CanvasBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="64" />
            <RowDefinition Height="*" />
            <RowDefinition Height="32" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource NavBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Padding="20,0" ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="232" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Spacing="1">
                    <TextBlock FontSize="17" FontWeight="SemiBold" Text="UNIFIED CONTROL" />
                    <TextBlock Style="{StaticResource SecondaryTextStyle}" FontSize="11" Text="TEST • AUTH • PROVISION" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Spacing="18">
                    <StackPanel Orientation="Horizontal" Spacing="7">
                        <Ellipse x:Name="ConnectionDot" Width="8" Height="8" Fill="{StaticResource ErrorBrush}" VerticalAlignment="Center" />
                        <TextBlock x:Name="ConnectionText" Text="Disconnected" VerticalAlignment="Center" />
                    </StackPanel>
                    <TextBlock x:Name="DeviceTypeText" Style="{StaticResource SecondaryTextStyle}" Text="No hardware selected" />
                    <TextBlock x:Name="SerialText" Style="{StaticResource DataTextStyle}" Text="SERIAL —" />
                    <Border x:Name="AuthBadge" Background="#332B3746" CornerRadius="12" Padding="10,4">
                        <TextBlock x:Name="AuthText" FontSize="12" Text="NOT AUTHORIZED" />
                    </Border>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Spacing="8">
                    <Button Click="SettingsButton_Click" ToolTipService.ToolTip="Connection and application settings">
                        <StackPanel Orientation="Horizontal" Spacing="7"><FontIcon Glyph="&#xE713;" /><TextBlock Text="Settings" /></StackPanel>
                    </Button>
                    <Button x:Name="DisconnectButton" Click="DisconnectButton_Click" Style="{StaticResource DangerButtonStyle}" IsEnabled="False" Content="Disconnect" />
                </StackPanel>
            </Grid>
        </Border>

        <NavigationView x:Name="Navigation" Grid.Row="1" PaneDisplayMode="Left" IsBackButtonVisible="Collapsed" IsSettingsVisible="False"
                        OpenPaneLength="252" Background="{StaticResource CanvasBrush}" SelectionChanged="Navigation_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Overview" Tag="overview" Icon="Home" />
                <NavigationViewItem Content="Device identity" Tag="identity"><NavigationViewItem.Icon><FontIcon Glyph="&#xE77B;" /></NavigationViewItem.Icon></NavigationViewItem>
                <NavigationViewItem Content="Keygen &amp; provisioning" Tag="keygen"><NavigationViewItem.Icon><FontIcon Glyph="&#xE72E;" /></NavigationViewItem.Icon></NavigationViewItem>
                <NavigationViewItem Content="Authorization" Tag="authorization"><NavigationViewItem.Icon><FontIcon Glyph="&#xE785;" /></NavigationViewItem.Icon></NavigationViewItem>
                <NavigationViewItem Content="Test center" Tag="tests"><NavigationViewItem.Icon><FontIcon Glyph="&#xE9D9;" /></NavigationViewItem.Icon></NavigationViewItem>
                <NavigationViewItem Content="4-phase workflow" Tag="firmware"><NavigationViewItem.Icon><FontIcon Glyph="&#xE896;" /></NavigationViewItem.Icon></NavigationViewItem>
                <NavigationViewItemSeparator />
                <NavigationViewItem Content="ASM controls" Tag="asm"><NavigationViewItem.Icon><FontIcon Glyph="&#xE950;" /></NavigationViewItem.Icon></NavigationViewItem>
                <NavigationViewItem Content="Stepper controls" Tag="stepper"><NavigationViewItem.Icon><FontIcon Glyph="&#xEC4A;" /></NavigationViewItem.Icon></NavigationViewItem>
                <NavigationViewItem Content="Reports &amp; logs" Tag="reports"><NavigationViewItem.Icon><FontIcon Glyph="&#xE9F9;" /></NavigationViewItem.Icon></NavigationViewItem>
            </NavigationView.MenuItems>
            <Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid x:Name="PageHost" Padding="24" />
                </ScrollViewer>
            </Grid>
        </NavigationView>

        <Border Grid.Row="2" Background="{StaticResource NavBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="16,0">
            <Grid>
                <TextBlock x:Name="StatusText" VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Text="Ready. Select a hardware profile or connect a device." />
                <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Text="UnifiedLicGen • Material Dark" />
            </Grid>
        </Border>

        <Popup x:Name="SettingsPopup" IsLightDismissEnabled="True">
            <Border x:Name="SettingsDrawer" Width="420" Background="{StaticResource RaisedBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="12" Padding="20" Translation="0,0,32">
                <ScrollViewer MaxHeight="760">
                    <StackPanel Spacing="18">
                        <Grid>
                            <StackPanel><TextBlock Style="{StaticResource PageTitleStyle}" Text="Settings" /><TextBlock Style="{StaticResource SecondaryTextStyle}" Text="Connection, polling and service options" /></StackPanel>
                            <Button HorizontalAlignment="Right" VerticalAlignment="Top" Click="CloseSettings_Click" Content="Close" />
                        </Grid>
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel Spacing="12">
                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Connection" />
                                <ComboBox x:Name="HardwareProfileBox" Header="Hardware profile" SelectedIndex="0">
                                    <ComboBoxItem Content="Auto detect" Tag="auto" />
                                    <ComboBoxItem Content="ASM I/O Card" Tag="asm" />
                                    <ComboBoxItem Content="Stepper Motion Card" Tag="stepper" />
                                </ComboBox>
                                <Grid ColumnSpacing="8"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <ComboBox x:Name="PortBox" Header="COM port" PlaceholderText="No ports found" />
                                    <Button Grid.Column="1" VerticalAlignment="Bottom" Click="RefreshPorts_Click" Content="Refresh" />
                                </Grid>
                                <Grid ColumnSpacing="8"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                    <ComboBox x:Name="BaudBox" Header="Baud" SelectedIndex="2"><ComboBoxItem Content="9600" /><ComboBoxItem Content="19200" /><ComboBoxItem Content="38400" /><ComboBoxItem Content="115200" /></ComboBox>
                                    <NumberBox x:Name="SlaveIdBox" Grid.Column="1" Header="Slave ID" Value="2" Minimum="1" Maximum="247" SpinButtonPlacementMode="Compact" />
                                </Grid>
                                <Button Click="ConnectButton_Click" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" Content="Connect and identify" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel Spacing="12">
                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Polling &amp; behavior" />
                                <NumberBox Header="Live status interval (ms)" Value="300" Minimum="100" Maximum="5000" SpinButtonPlacementMode="Compact" />
                                <ToggleSwitch Header="Reconnect monitoring" IsOn="True" />
                                <ToggleSwitch Header="Confirm motion commands" IsOn="True" />
                                <ToggleSwitch Header="Include backend authorization" IsOn="False" />
                            </StackPanel>
                        </Border>
                        <Expander Header="Service and diagnostics">
                            <StackPanel Spacing="10" Padding="0,10,0,0">
                                <PasswordBox x:Name="MasterPasswordBox" Header="Temporary service unlock" PlaceholderText="Service password" />
                                <Button Click="ServiceUnlock_Click" Content="Unlock for this session" />
                                <ToggleSwitch Header="Show raw protocol diagnostics" />
                                <Button Content="Open log folder" />
                            </StackPanel>
                        </Expander>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Popup>
    </Grid>
</Window>
